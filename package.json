{
  "name": "enlace-constructor-pro",
  "private": true,
  "version": "1.7.0",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "verify:canonical": "node scripts/verify_canonical.js",
    "canonical:accept": "node scripts/accept_canonical_baseline.js",
    "prebuild": "npm run verify:canonical",
    "build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.6.0",
    "acorn-jsx": "^5.3.2",
    "cors-anywhere": "^0.4.4",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.6.0",
    "acorn": "^8.17.0",
    "playwright-core": "^1.62.0"
  }
}
